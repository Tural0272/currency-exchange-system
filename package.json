{
  "name": "currency-exchange-system",
  "version": "1.0.0",
  "description": "Currency Exchange System - Mobile App with REST Backend",
  "private": true,
  "scripts": {
    "setup": "npm install && npm run setup:backend && npm run setup:mobile && npm run db:init",
    "setup:backend": "cd backend && npm install",
    "setup:mobile": "cd mobile && npm install && npx expo install --fix",
    "db:init": "cd backend && npm run db:init",
    "dev": "npm-run-all --parallel dev:backend dev:mobile",
    "dev:backend": "cd backend && npm run dev",
    "dev:mobile": "cd backend && npx wait-on http://localhost:3010/health && cd ../mobile && npm start",
    "web": "cd mobile && npx expo start --web",
    "web:open": "start http://localhost:8081"
  },
  "keywords": [],
  "author": "Tural Alakbarov",
  "license": "ISC",
  "devDependencies": {
    "wait-on": "^7.2.0",
    "npm-run-all": "^4.1.5"
  }
}
